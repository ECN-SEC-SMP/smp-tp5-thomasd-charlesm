#include "type_def.h"
#include "utils.h"

int main() {
    personne* john = creerPersonne('D', 'J', 1990, 1, nullptr, nullptr, nullptr);
    personne* jane = creerPersonne('S', 'J', 1992, 2, nullptr, nullptr, nullptr);
}